# DEWDROP runtime dependencies
httpx>=0.27          # async HTTP for source pollers + GW2000
fastapi>=0.110       # read-only API queried by Berries
uvicorn[standard]>=0.29
python-dotenv>=1.0   # .env loading (mirrors Berries config pattern)

# Dev/test
pytest>=8.0
