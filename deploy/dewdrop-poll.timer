[Unit]
Description=Snapshot DEWDROP forecasts nightly (design doc §4 Phase 1)

[Timer]
# Just after midnight local time; catch up if the machine was off.
OnCalendar=*-*-* 00:10:00 America/Chicago
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
